info:
  name: hour-types
  type: http
  seq: 8

http:
  method: GET
  url: "{{px_url}}/api/hour-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
