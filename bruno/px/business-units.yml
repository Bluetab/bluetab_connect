info:
  name: business-units
  type: http
  seq: 6

http:
  method: GET
  url: "{{px_url}}/api/business-units"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
