info:
  name: spend-types
  type: http
  seq: 9

http:
  method: GET
  url: "{{px_url}}/api/spend-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
