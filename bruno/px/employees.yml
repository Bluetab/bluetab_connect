info:
  name: employees
  type: http
  seq: 1

http:
  method: GET
  url: "{{px_url}}/api/employees?employee_number=10001"
  params:
    - name: employee_number
      value: "10001"
      type: query
    - name: email
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
