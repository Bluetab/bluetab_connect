info:
  name: employees
  type: http
  seq: 1

http:
  method: GET
  url: "{{px_url}}/api/employees"
  params:
    - name: employee_number
      value: "1001"
      type: query
      disabled: true
    - name: email
      value: employee@example.com
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
