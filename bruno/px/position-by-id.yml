info:
  name: position-by-id
  type: http
  seq: 12

http:
  method: GET
  url: "{{px_url}}/api/positions/10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
