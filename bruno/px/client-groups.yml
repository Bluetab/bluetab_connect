info:
  name: client-groups
  type: http
  seq: 4

http:
  method: GET
  url: "{{px_url}}/api/client-groups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
