info:
  name: initiatives
  type: http
  seq: 2

http:
  method: GET
  url: "{{px_url}}/api/initiatives"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
