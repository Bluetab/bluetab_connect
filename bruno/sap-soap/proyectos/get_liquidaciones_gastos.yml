info:
  name: get_liquidaciones_gastos
  type: http
  seq: 11

http:
  method: POST
  url: "{{sap_soap_url}}/{{endpoint}}"
  headers:
    - name: SOAPAction
      value: http://SCL/SCL.Addins.Bluetab.Proyectos/WebServices/GetLiquidacionesGastos
    - name: Content-Type
      value: text/xml;charset=utf-8
  body:
    type: xml
    data: |-
      <?xml version="1.0" encoding="UTF-8"?>
      <env:Envelope xmlns:env="http://schemas.xmlsoap.org/soap/envelope/" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
          <env:Header/>
          <env:Body>
              <GetLiquidacionesGastos xmlns="http://SCL/SCL.Addins.Bluetab.Proyectos/WebServices">
                  <request>
                      <AuthenticationToken>{{soap_token}}</AuthenticationToken>
                      <UserId>{{user_id}}</UserId>
                  </request>
              </GetLiquidacionesGastos>
          </env:Body>
      </env:Envelope>
  auth: inherit

runtime:
  variables:
    - name: user_id
      value: "122"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
