info:
  name: WSDL
  type: http
  seq: 10

http:
  method: GET
  url: "{{sap_soap_url}}/{{endpoint}}?WSDL"
  params:
    - name: WSDL
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
